apiVersion: v1
kind: Service
metadata:
  name: iyktjfhdr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "iyktjfhdr"
    port: 8080
  selector:
    app: iyktjfhdr
